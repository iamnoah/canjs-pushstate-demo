# PushState Demo Application

A simple server to demo push state. The server is so that the different pushed URLs still resolve to the demo page.

Make sure node and npm are installed then:

    npm start
